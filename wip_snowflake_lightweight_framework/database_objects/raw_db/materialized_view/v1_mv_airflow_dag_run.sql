!SET variable_substitution=true;
USE ROLE &{PROGRAM}_&{ENV}_DATA_LOADER;
USE WAREHOUSE &{PROGRAM}_&{ENV}_LOADING_WH;
USE DATABASE &{PROGRAM}_&{ENV}_RAW_DB;
USE SCHEMA UTILITIES;

CREATE MATERIALIZED VIEW IF NOT EXISTS MV_AIRFLOW_DAG_RUN_HEADER AS (
    SELECT DISTINCT
        DAG_NAME
        , DAG_RUN
        , DAG_RUN_STATE
        , DAG_RUN_START_DATE
        , DAG_RUN_END_DATE
        , DAG_RUN_DURATION
    FROM AIRFLOW_DAG_RUN
);
